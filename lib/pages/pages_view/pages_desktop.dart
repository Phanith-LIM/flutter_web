import 'package:flutter/material.dart';

class PagesDesktop extends StatelessWidget {
  const PagesDesktop({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
