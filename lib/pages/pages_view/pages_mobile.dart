import 'package:flutter/material.dart';

class PagesMobile extends StatelessWidget {
  const PagesMobile({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
